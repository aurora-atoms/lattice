# Synthetic Dangling-Reference Review Extension

This public fixture simulates a private task-scoped extension. It adds one bounded check: reject manager claims whose evidence references do not resolve inside the local synthetic ledger.

It does not override the public capability, authorize promotion, or represent real private content.
