# Context Routing

```text
CTX.001 | MUST  | handoff | use JSONL + JSON Schema for agent handoff
CTX.002 | MUST  | runtime | use append-only JSONL for runtime ledgers
CTX.003 | SHOULD| report  | use Markdown for manager-facing dossiers
CTX.004 | NEVER | prompt  | expose raw logs, traces, diffs, or bulk tool output by default
CTX.005 | MUST  | prompt  | use bounded context projection before model-visible context
```
